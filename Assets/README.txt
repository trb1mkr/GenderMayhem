Если в анимации не поставить начальный фрейм (0), 
то анимация перескочит в конец

Достаточно иметь 1 анимацию, где анимируется переменная, 
чтобы переменную нельзя было изменять через скрипт 

Avoid коллайдер запрещает бить и стрелять, но при этом 
если удар уже в процессе, то он не останавливается

Если проблемы с производительностью из-за физики, то смотри 
в Time - Fixed timestep